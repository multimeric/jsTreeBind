{
  "name": "jsTree-bind",
  "version": "0.0.1",
  "devDependencies": {
    "rimraf": "^2.2.8",
    "uglify-js": "^2.4.16"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TMiguelT/jsTree-bind.git"
  },
  "scripts": {
    "build": "mkdir dist && uglifyjs src/jsTreeBind.js -m -c -o dist/jsTreeBind.min.js",
    "clean": "rimraf dist"
  }
}
